[color]
        diff = auto
        status = auto
        branch = auto
[merge]
        summary = true
        tool = emerge
[alias]
        ci = commit
        co = checkout
        diffc = diff --cached
        rb = !grb
        who = shortlog -s -n
        pt = push --tags origin master
        pullr = pull --rebase

[user]
        name = Christopher Grim
